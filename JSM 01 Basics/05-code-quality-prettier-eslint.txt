Different developers have their own OPINION about how to write code.
These creates sometimes some bugs, syntax errors and may sometimes break the code entirely
  - single vs double quotes
  - how much to indent lines of code
  - put/not put starting curly brace in the same line as the function name



ESLint and Prettier
-------------------
  ESLint - gives errors and shows bugs; for code quality
    >> linter tool for ECMAScript
    >> identify unused variables
    >> confusing scoping
    - gives the fixed code in side panel
    - ESLint for React, Vue ro Angular (plugin based)

  Prettier - visual formmatting fo the code



  Both have configurations that can be customized to my own coding preferences



  -----------------------------------------------------
  .eslintrc (.gitignore) [sometimes hidden by the OS]
    {
      "extends": [

      ]
    }

  then install the extension